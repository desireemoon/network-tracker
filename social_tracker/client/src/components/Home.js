import React from 'react';
import { withRouter } from 'react-router-dom';

const Home = (props) => {

    return (
      <div className="">
       Welcome home friend!
      </div>
    );
  }
  
  export default withRouter(Home);